import 'package:flutter/material.dart';

class ThingTile extends StatelessWidget {
  const ThingTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}